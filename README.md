# taurophylax.github.io

Portfolio site hosted with GitHub Pages.

## Setup status

- Root `index.html` placeholder is present (replace with your final portfolio HTML).
- `.nojekyll` is included so files are served as static content without Jekyll processing.
